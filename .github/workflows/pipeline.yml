name: Tinycloud Backend Pipeline
on:
    push:
        branches: [main]
env:
    AWS_REGION: eu-north-1
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
concurrency:
    group: tinycloud-backend-pipeline
    cancel-in-progress: true
jobs:
    build:
        runs-on: ubuntu-latest
        permissions:
          contents: write
        outputs:
          version: ${{ steps.semver.outputs.new_tag }}
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                fetch-depth: 0

            - name: Semver action
              id: semver
              uses: anothrNick/github-tag-action@v1
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                DEFAULT_BUMP: patch

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v5
              with:
                aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                aws-region: ${{ env.AWS_REGION }}

            - name: Login to Amazon ECR
              id: login-ecr
              uses: aws-actions/amazon-ecr-login@v2

            - name: Build and Push Docker image
              run: |
                docker build --no-cache -t tinycloud-backend:latest .
                docker tag tinycloud-backend:latest ${{ steps.login-ecr.outputs.registry }}/tinycloud-docker-backend:${{ steps.semver.outputs.new_tag }}
                docker push ${{ steps.login-ecr.outputs.registry }}/tinycloud-docker-backend:${{ steps.semver.outputs.new_tag }}

            - name: Build and Push Helm chart
              run: |
                helm dependency update charts/tinycloud-backend
                helm package charts/tinycloud-backend --version ${{ steps.semver.outputs.new_tag }}
                helm push tinycloud-backend-*.tgz oci://${{ steps.login-ecr.outputs.registry }}
            
            - name: Deploy dev
              run: |
                gh workflow run pipeline.yaml \
                  --repo cherya05/tinycloud-infra \
                  -f environment=dev \
                  -f service=backend \
                  -f version=${{ steps.semver.outputs.new_tag }}
              env:
                GH_TOKEN: ${{ secrets.GH_TOKEN }}
