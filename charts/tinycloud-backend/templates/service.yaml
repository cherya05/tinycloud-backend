apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinycloud-backend.fullname" . }}
  labels:
    {{- include "tinycloud-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinycloud-backend.selectorLabels" . | nindent 4 }}