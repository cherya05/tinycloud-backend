apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinycloud.fullname" . }}
  labels:
    {{- include "tinycloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinycloud.selectorLabels" . | nindent 4 }}