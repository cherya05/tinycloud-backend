provider:
  name: aws

extraArgs:
  - --aws-zone-type=public
  - --domain-filter=hatstore-danil.info
  - --txt-owner-id=tinycloud-dev-id
  - --txt-prefix=external-dns-
  - --namespace=tinycloud-dev

env:
  - name: AWS_REGION
    value: "eu-north-1"
  - name: EXTERNAL_DNS_RESOLVE_SERVICE_LOAD_BALANCER_HOSTNAME
    value: "true"

nodeSelector:
  environment: "dev"

tolerations:
  - key: "environment"
    operator: "Equal"
    value: "dev"
    effect: "NoSchedule"

resources:
  limits:
    memory: 128Mi
    cpu: 100m
  requests:
    memory: 64Mi
    cpu: 50m